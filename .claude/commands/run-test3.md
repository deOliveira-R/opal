Run OPAL Feasibility Test 3 — MSL/Fluid compatibility (StaticPipe, DynamicPipe, two-phase water).

Loads Modelica Standard Library (~60s). Tests IF97 media extraction (FM3).

```bash
cd /Users/rodrigo/git/OPAL/feasibility && ../external/venv/bin/python test3_fluid_compat.py
```
