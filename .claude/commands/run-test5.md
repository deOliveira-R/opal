Run OPAL Feasibility Test 5 — 3D array component extraction (Approach B vessel).

Tests mesh sizes 2×2×3, 3×3×5, 4×4×8. Checks FM1 (loop unrolling), FM2 (BLT coherence), FM4 (scaling), FM5 (array index names).

```bash
cd /Users/rodrigo/git/OPAL/feasibility && ../external/venv/bin/python test5_3d_array.py
```
