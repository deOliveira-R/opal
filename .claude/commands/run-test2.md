Run OPAL Feasibility Test 2 — Information completeness (adjacency matrix, events, stream connectors, name traceability).

Requires Test 1 to have run first (reads its backEnd XML for Part A).

```bash
cd /Users/rodrigo/git/OPAL/feasibility && ../external/venv/bin/python test2_information_completeness.py
```
