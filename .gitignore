# Build outputs
external/OpenModelica/build_cmake/

# Python virtualenv
external/venv/

# External Modelica libraries (cloned for reference, not tracked)
external/TRANSFORM/
external/ThermoPower/
external/ThermoSysPro/
external/MSL/
external/Buildings/
external/OpenIPSL/

# Feasibility test outputs (large XML / flat Modelica files)
feasibility/results/*.xml
feasibility/results/*.mo
feasibility/results/*.c
feasibility/results/*.h
feasibility/results/*.bin
feasibility/results/*.json
feasibility/results/*.makefile
feasibility/results/report.txt

# Solver build artifacts
solver/single_phase/build/
solver/single_phase/*.so

# Python
__pycache__/
*.pyc
*.pyo

# Temporary Modelica probe files
library/Media/tests/_probe.mo

# Claude Code local settings (machine-specific paths)
.claude/settings.local.json

# macOS
.DS_Store
.gitnexus
solver/two_phase/build/
*.so
external/ClaRa/
